---
name: "Jellyfin Startup Ads"
guid: "6d1a9b6e-6b3e-4f9a-9c2d-3a7f1e2c4b90"
version: "1.4.1.0"
targetAbi: "10.11.0.0"
framework: "net9.0"
owner: "franciscocolon22"
overview: "Anuncios al iniciar Jellyfin Web y pre-roll antes de cada vídeo."
description: >
  Dos sistemas de anuncios independientes: un overlay multimedia (imagen, vídeo o texto)
  al abrir Jellyfin Web, y un pre-roll que reproduce vídeos de la biblioteca antes de cada
  película o episodio (funciona también en apps nativas via IIntroProvider). Administrable
  desde el Dashboard, con countdown, omitir, programación, segmentación por usuario,
  prioridades, aleatorio y directorio externo.
category: "General"
artifacts:
  - "Jellyfin.Plugin.StartupAds.dll"
changelog: >-
  v1.4.1 — Configuración reorganizada en 3 bloques claros: «1 · General» (aplica a
  los dos sistemas), «2 · Presentación» (overlay web) y «3 · Pre-roll». El pre-roll
  gana su propia «Carpeta de vídeos» con «Validar carpeta» y «Escanear e importar
  vídeos»; el buscador de vídeos se limita a esa carpeta. El pre-roll ahora también
  envía la ruta del vídeo (IntroInfo.Path) y descarta anuncios cuyo vídeo ya no
  existe. Incluye v1.4.0 y v1.3.4.
